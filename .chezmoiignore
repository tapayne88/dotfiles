{{- if eq "darwin" .chezmoi.os }}
.local/share
{{- end }}

# Crostini / chrome os patches
{{- if ne "penguin" .chezmoi.hostname }}
.config/zsh/functions/patch-crostini.zsh
{{- end }}

# Ignore windows terminal config on anything not windows
{{- if (not (regexMatch "-microsoft-" .chezmoi.kernel.osrelease)) }}
.config/windows_terminal
{{- end }}

misc
README.md
debian_installation.md
terminfo
install.sh
update_jet_brains_mono.sh
git_template

# Ignore icons with kitty config
.config/kitty/icons

# Ignore window font folder
.local/share/fonts/windows
