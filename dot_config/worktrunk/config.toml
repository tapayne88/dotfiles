worktree-path = "../.worktrees/{{ repo }}.{{ branch | codename(2) }}"

[post-start]
copy = "wt step copy-ignored"

[pre-remove]
tmux = """
NAME={{ repo }}/{{ branch | codename(2) }}
tmux kill-session -t "$NAME" 2>/dev/null || true
"""
