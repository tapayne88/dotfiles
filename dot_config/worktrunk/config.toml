worktree-path = "../.worktrees/{{ repo }}.{{ branch | hash }}"

[post-start]
copy = "wt step copy-ignored"

[pre-remove]
tmux = """
NAME={{ repo }}/{{ branch }}
tmux kill-session -t "$NAME" 2>/dev/null || true
"""
